import { BicycleAttributeDetail } from './bicycle-attribute-detail.entity';

describe('BicycleAttributeDetail', () => {
  it('should be defined', () => {
    expect(new BicycleAttributeDetail()).toBeDefined();
  });
});
