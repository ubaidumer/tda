import type { BranchUpgradeConfig } from '../../types';
import type { ChangeLogResult } from './types';
export declare function getChangeLogJSON({ versioning, currentVersion, newVersion, sourceUrl, releases, depName, manager, sourceDirectory, }: BranchUpgradeConfig): Promise<ChangeLogResult | null>;
