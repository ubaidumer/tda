import type { BranchUpgradeConfig } from '../../types';
import { ChangeLogResult } from './types';
export declare function getChangeLogJSON({ versioning, currentVersion, newVersion, sourceUrl, sourceDirectory, releases, depName, manager, }: BranchUpgradeConfig): Promise<ChangeLogResult | null>;
