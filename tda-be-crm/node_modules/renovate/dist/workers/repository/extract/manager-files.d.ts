import type { RenovateConfig } from '../../../config/types';
import type { PackageFile } from '../../../manager/types';
export declare function getManagerPackageFiles(config: RenovateConfig): Promise<PackageFile[]>;
