export declare class CommitMessage {
    static readonly SEPARATOR: string;
    private message;
    private prefix;
    constructor(message?: string);
    static formatPrefix(prefix: string): string;
    setMessage(message: string): void;
    setCustomPrefix(prefix?: string): void;
    setSemanticPrefix(type?: string, scope?: string): void;
    toString(): string;
    private formatMessage;
}
