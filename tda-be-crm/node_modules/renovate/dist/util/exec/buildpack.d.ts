import type { ToolConstraint } from './types';
export declare function resolveConstraint(toolConstraint: ToolConstraint): Promise<string>;
export declare function generateInstallCommands(toolConstraints: ToolConstraint[]): Promise<string[]>;
