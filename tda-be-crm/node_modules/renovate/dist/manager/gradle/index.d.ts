import { ProgrammingLanguage } from '../../constants';
import type { ExtractConfig, PackageFile, UpdateDependencyConfig } from '../types';
import type { GradleManagerData } from './types';
export declare function extractAllPackageFiles(config: ExtractConfig, packageFiles: string[]): Promise<PackageFile[] | null>;
export declare function updateDependency(params: UpdateDependencyConfig<GradleManagerData>): string | null;
export declare const language = ProgrammingLanguage.Java;
export declare const defaultConfig: {
    fileMatch: string[];
    timeout: number;
    versioning: string;
};
