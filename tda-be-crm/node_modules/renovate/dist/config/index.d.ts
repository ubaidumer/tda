import type { AllConfig, RenovateConfig, RenovateConfigStage } from './types';
import { mergeChildConfig } from './utils';
export { mergeChildConfig };
export declare function getManagerConfig(config: RenovateConfig, manager: string): RenovateConfig;
export declare function filterConfig(inputConfig: AllConfig, targetStage: RenovateConfigStage): AllConfig;
