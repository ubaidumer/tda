import type { Preset, PresetConfig } from '../types';
export declare const Endpoint = "https://api.github.com/";
export declare function fetchJSONFile(repo: string, fileName: string, endpoint: string, packageTag?: string): Promise<Preset>;
export declare function getPresetFromEndpoint(pkgName: string, filePreset: string, presetPath: string, endpoint?: string, packageTag?: string): Promise<Preset>;
export declare function getPreset({ packageName: pkgName, presetName, presetPath, packageTag, }: PresetConfig): Promise<Preset>;
