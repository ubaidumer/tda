import type { Preset, PresetConfig } from '../types';
export declare const Endpoint = "https://gitlab.com/api/v4/";
export declare function fetchJSONFile(repo: string, fileName: string, endpoint: string, packageTag?: string): Promise<Preset>;
export declare function getPresetFromEndpoint(pkgName: string, presetName: string, presetPath: string, endpoint?: string, packageTag?: string): Promise<Preset>;
export declare function getPreset({ packageName: pkgName, presetPath, presetName, packageTag, }: PresetConfig): Promise<Preset>;
