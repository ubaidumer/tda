import type { Preset } from '../types';
export declare function fetchJSONFile(repo: string, fileName: string, _endpoint?: string): Promise<Preset>;
export declare function getPresetFromEndpoint(pkgName: string, filePreset: string, presetPath: string, endpoint: string): Promise<Preset>;
