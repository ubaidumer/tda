import type { DigestAsset, GithubRelease } from './types';
/** Identify the asset associated with a known digest. */
export declare function findDigestAsset(release: GithubRelease, digest: string): Promise<DigestAsset>;
/** Given a digest asset, find the equivalent digest in a different release. */
export declare function mapDigestAssetToRelease(digestAsset: DigestAsset, release: GithubRelease): Promise<string | null>;
