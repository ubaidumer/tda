import { Datasource } from '../datasource';
import type { GetReleasesConfig, ReleaseResult } from '../types';
import type { RepositoryData } from './types';
export declare class HelmDatasource extends Datasource {
    static readonly id = "helm";
    constructor();
    readonly defaultRegistryUrls: string[];
    readonly defaultConfig: {
        commitMessageTopic: string;
        group: {
            commitMessageTopic: string;
        };
    };
    readonly defaultVersioning = "helm";
    getRepositoryData(repository: string): Promise<RepositoryData | null>;
    getReleases({ lookupName, registryUrl: helmRepository, }: GetReleasesConfig): Promise<ReleaseResult | null>;
}
