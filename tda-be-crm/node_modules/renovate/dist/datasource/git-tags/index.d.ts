import { Datasource } from '../datasource';
import type { DigestConfig, GetReleasesConfig, ReleaseResult } from '../types';
export declare class GitTagsDatasource extends Datasource {
    static readonly id = "git-tags";
    constructor();
    readonly customRegistrySupport = false;
    getReleases({ lookupName, }: GetReleasesConfig): Promise<ReleaseResult | null>;
    getDigest({ lookupName }: DigestConfig, newValue?: string): Promise<string | null>;
}
