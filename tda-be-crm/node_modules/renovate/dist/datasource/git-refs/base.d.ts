import type { GetReleasesConfig } from '../types';
import type { RawRefs } from './types';
export declare class GitDatasource {
    static id: string;
    static getRawRefs({ lookupName }: GetReleasesConfig, hostType: string): Promise<RawRefs[] | null>;
}
